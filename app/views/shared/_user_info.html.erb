<div class="mt-3" style="font-size:2.0rem">
  <%= link_to gravatar_for(current_user, size: 60), current_user %>
  <%= current_user.name %>
</div>
<div class="mx-5">
<span><%= link_to "view my profile", current_user %></span>
<p><%= pluralize(current_user.microposts.count, "micropost") %></p>
</div>
