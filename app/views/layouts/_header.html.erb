<div class="navbar navbar-expand navbar-light bg-light">
  <div class="container justify-content-start">
    <%= link_to "Sample app", root_path, class: 'navbar-brand' %>
    <nav>
      <ul class="nav navbar-nav navbar-right">
        <li><%= link_to "Home", root_path, class: 'btn btn-light' %></li>
        <li><%= link_to "Help", help_path, class: 'btn btn-light' %></li>
        <% if logged_in? %>
          <li><%= link_to "Users", users_path, class: 'btn btn-light' %></li>

          <li><%= link_to "Profile", current_user, class: 'btn btn-light' %></li>
          <li><%= link_to "Settings", edit_user_path(current_user), class: 'btn btn-light' %></li>

          <li><%= button_to "Log out", logout_path, method: "delete", class: 'btn btn-light' %></li>
        <% else %>
          <li><%= link_to "Log in", login_path, class: 'btn btn-light' %></li>
        <% end %>
      </ul>
    </nav>
  </div>
</div>
</header>
